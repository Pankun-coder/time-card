<header class="navbar navbar-fixed-top navbar-inverse mx-auto bg-black bg-opacity-50">
  <div class="mx-auto w-full text-center">
    <%=  link_to("打刻アプリ(仮)", root_path, {id: "logo", class: "font-bold text-5xl text-white"}) %>
    <nav>
      <ul class="nav navbar-nav navbar-right mx-auto bg-black bg-opacity-50 text-white">
        <li class="inline-block mx-6"><%= link_to "Home", root_path %></li>
        <li class="inline-block mx-6"><%= link_to "Help", help_path %></li>
        <% if logged_in? %>
          <li class="inline-block mx-6"><%= link_to "Users", '#' %></li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              Account <b class="caret"></b>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to "Profile", current_user %></li>
              <li><%= link_to "Settings", '#' %></li>
              <li class="divider"></li>
              <li>
                <%= link_to "Log out", logout_path, data: {turbo_method: :delete} %>
              </li>
            </ul>
          </li>
        <% else %>
          <li class="inline-block mx-6"><%= link_to "Log in", login_path %></li>
        <% end %>
      </ul>
    </nav>
  </div>
</header>